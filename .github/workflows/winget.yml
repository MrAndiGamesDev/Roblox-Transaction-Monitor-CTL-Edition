name: Publish to Winget

on:
  release:
    types: [released]

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@main
        with:
          identifier: MrAndiGamesDev.RobloxTransactionMonitorCTLEdition
          token: ${{ secrets.WINGET_TOKEN }}